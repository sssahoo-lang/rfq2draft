ANTHROPIC_API_KEY=

# Optional: real email sending via Gmail (off by default; the pipeline mocks
# the send unless BOTH of these are set). Use a Gmail App Password, which
# requires 2-Step Verification on the account. Never commit real values.
GMAIL_ADDRESS=
GMAIL_APP_PASSWORD=
