[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "rfq-agent"
version = "0.1.0"
description = "RFQ-to-quote drafting agent prototype"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
